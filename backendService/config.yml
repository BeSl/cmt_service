project:
  name: LocalCommit
  debug: true
  environment: development

status:
  port: 8081

database:
    host: localhost
    port: 5445
    user: user
    password: user
    name: mdb
    sslmode: disable
    migrations: migrations
    driver: pgx

commitserver:
    host: localhost
    port: 4000
    api: "api/v1/new"